{
  "name": "cti-manager",
  "version": "1.3.0",
  "description": "Nothing here yet",
  "main": "index.js",
  "scripts": {
    "build": "gulp build",
    "dev": "gulp dev --dev | bunyan",
    "test": "gulp qa",
    "version": "node version",
    "postinstall": "cd cti-manager-server && npm install && cd ../cti-manager-web-ui && npm install && cd .."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "bunyan": "^1.8.1",
    "eslint": "^3.19.0",
    "gulp": "^3.9.1",
    "lodash": "^4.15.0"
  }
}
