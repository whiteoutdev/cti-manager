import {Tease} from '../model/tease/Tease';
import User from '../model/user/User';
import {TeaseConfig} from '../model/tease/TeaseConfig';

class TeaseManager {
    public createTease(user: User, config: TeaseConfig): Tease {
        // TODO
        return null;
    }
}

export default new TeaseManager();
