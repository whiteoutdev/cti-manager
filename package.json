{
  "name": "cti-manager",
  "version": "1.2.0",
  "description": "Nothing here yet",
  "main": "index.js",
  "scripts": {
    "build": "concurrently \"gulp build\" \"cd cti-manager-web-ui && yarn build\"",
    "dev": "gulp dev --dev | bunyan",
    "test": "gulp qa",
    "version": "node version",
    "postinstall": "cd cti-manager-server && yarn && cd ../cti-manager-web-ui && yarn && cd ..",
    "start": "yarn build && cd cti-manager-server && yarn start",
    "stop": "cd cti-manager-server && yarn stop",
    "restart": "yarn build && cd cti-manager-server && yarn restart"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "bunyan": "^1.8.1",
    "eslint": "^3.19.0",
    "gulp": "^3.9.1",
    "lodash": "^4.15.0"
  },
  "dependencies": {
    "concurrently": "^3.4.0"
  }
}
